# Writeflow Frontend Environment Variables
# Copy this file to .env and update the values

# API URL - Get this from `sam deploy` outputs (ApiUrl)
# For local development without backend, you can leave the default
VITE_API_URL=http://localhost:3000

# Production example:
# VITE_API_URL=https://your-api-id.execute-api.us-east-1.amazonaws.com/dev

# Note: API Key is entered by users via dialog (stored in localStorage)
# Generate keys with: ./scripts/create-api-key.sh
